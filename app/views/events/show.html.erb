<br>

<h2>Your Events</h2>
<table>
  <thead>
    <tr>
      <th>Description</th>
      <th>Location</th>
      <th>Date</th>
      <th>Attendees</th>
    </tr>
  </thead>

  <tbody>    
    <% @event.each do |e| %>
      <tr>
        <td><%= e.description %></td>
        <td><%= e.location %></td>
        <td><%= e.date %></td>
        <% e.attendees.each do |a| %>
          <td><%= a.username %></td>
        <% end %> 
      </tr>
    <% end %>
  </tbody>
</table>

<br><br>
<p>You have been invited to these <%= link_to 'Events', current_user %></p>
